interface IClient {
  id: string;
  firstName: string;
  lastName: string;
  updated: string;
  status: string;
  email: string;
  phone: string;
  // reps: [Rep];
  // notes: [Note];
  // tags: [Tag];
  // organizations: [Organization]
}